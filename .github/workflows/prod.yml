name: Deploy to Prod

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: sarah-vps
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cwd
        run: |
            echo "$PWD"