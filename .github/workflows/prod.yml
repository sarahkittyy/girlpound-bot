name: Deploy to Prod

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: sarah-vps
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: |
            cargo build --release
            cargo sqlx migrate run

      - name: Run
        run: |
            sudo supervisorctl restart girlpound-bot
